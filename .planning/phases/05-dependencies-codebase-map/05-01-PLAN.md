---
wave: 1
plan: 05-01
phase: 5
phase_slug: dependencies-codebase-map
depends_on: []
parallelization: false
autonomous: true
requirements_addressed:
  - DEP-01
  - DOC-03
---

# Plan 05-01: Dependencies & codebase map — `python-dotenv`, import audit, `.planning/codebase/*` refresh

## Objective

Close **DEP-01** by declaring **`python-dotenv`** and completing a **shallow runtime import audit** of shipped `src/contextcliff/`. Close **DOC-03** by refreshing **`.planning/codebase/*.md`** so the map matches **post–Phase 4** behavior ( **`import` CLI**, **`import_bridge/`**, **`profile_report`**, provenance-aware reporting, API-only story) **without** rewriting `docs/` outside what the map files already describe.

**Explicitly out of scope:** Removing unused deps from `pyproject.toml`, broad doc rewrites, architecture or code refactors.

## Requirements addressed

| ID | Coverage |
|----|----------|
| **DEP-01** | `python-dotenv` in `pyproject.toml`; shipped imports vs declared deps verified |
| **DOC-03** | `STACK`, `ARCHITECTURE`, `INTEGRATIONS`, `STRUCTURE` updated for Phases 3–4; `CONCERNS`, `CONVENTIONS`, `TESTING` only for factual fixes |

## Strategy

Single wave, **strictly ordered smallest safe change first**: lock dependencies before editing narrative docs; update **STACK** before other map files that reference the dependency table; touch **CONCERNS** early so the dotenv warning does not contradict **STACK**.

---

## Task 1 — `pyproject.toml`: declare `python-dotenv`

<action>

1. In **`pyproject.toml`**, inside **`[project].dependencies`**, add the string **`"python-dotenv"`** as a new array element.
2. Place it **immediately after** the **`"openai"`** line (keeps related “env + API client” deps adjacent; matches existing comment style).
3. Do **not** add version pins unless you add them for **all** runtime deps in the same file—preserve the current loose/unpinned style for consistency.

Exact target shape after edit (dependencies block only—the ellipsis is illustrative):

```toml
dependencies = [
    "click",
    ...
    "openai",
    "python-dotenv",
    "rich",
]
```

</action>

<read_first>

- `pyproject.toml`

</read_first>

<acceptance_criteria>

- `rg '"python-dotenv"' pyproject.toml` exits 0.
- `python-dotenv` appears exactly once in `[project].dependencies`.

</acceptance_criteria>

---

## Task 2 — Resolve env and smoke-import shipped entrypoints (DEP-01)

<action>

1. From the repo root **`/Users/aditya/Documents/projects/contextCLI`**, run **`uv sync`** so the workspace environment includes the new dependency.
2. Run:
   - **`uv run python -c "from dotenv import load_dotenv; print('dotenv_ok')"`** — must print **`dotenv_ok`**.
   - **`uv run python -c "from contextcliff.models.openai_client import OpenAIClient; from contextcliff.data.adapters.narrative_qa import NarrativeQAAdapter"`** — must exit **0** with no traceback.

</action>

<read_first>

- `pyproject.toml`

</read_first>

<acceptance_criteria>

- `uv sync` completes with exit code **0**.
- Both **`uv run python -c`** commands exit **0**; the first prints **`dotenv_ok`**.

</acceptance_criteria>

---

## Task 3 — Shallow import audit: shipped modules vs `pyproject.toml` (DEP-01)

<action>

1. Under **`src/contextcliff/`**, collect **third-party** top-level imports (exclude **`contextcliff`**, **`__future__`**, and stdlib modules: `abc`, `dataclasses`, `json`, `logging`, `os`, `random`, `sqlite3`, `sys`, `time`, `typing`, `pathlib` if present, etc.).
2. Expected third-party set after Task 1: **`click`**, **`datasets`**, **`dotenv`** (package name **`python-dotenv`**), **`numpy`**, **`openai`**, **`pandas`**, **`tiktoken`**.
3. Confirm **each** of the above appears as a **substring** of some line in **`[project].dependencies`** (`openai` → `"openai"`, `dotenv` → `"python-dotenv"`, etc.).
4. **Do not** change `pyproject.toml` in this task except if the audit finds a **runtime** import of a package **absent** from dependencies (unexpected—if found, add that single dep only; otherwise no edit).

</action>

<read_first>

- `pyproject.toml`
- `src/contextcliff/models/openai_client.py`
- `src/contextcliff/data/adapters/narrative_qa.py`
- `src/contextcliff/cli/main.py`

</read_first>

<acceptance_criteria>

- Third-party imports under **`src/contextcliff/`** resolve only to: **`click`**, **`datasets`**, **`dotenv`** (package **`python-dotenv`**), **`numpy`**, **`openai`**, **`pandas`**, **`tiktoken`** — verify with:  
  `rg '^\\s*(from|import) ' src/contextcliff --glob '*.py'` and exclude stdlib / **`contextcliff`** / **`__future__`** by inspection.
- Confirm declarations:  
  `rg -E '"click"|"datasets"|"numpy"|"openai"|"pandas"|"python-dotenv"|"tiktoken"' pyproject.toml` — seven distinct dependency strings present (one line each or as listed).
- If no missing third-party dep was found: **`pyproject.toml`** has **no** additional dependency lines added in Task 3 beyond Task 1.

</acceptance_criteria>

---

## Task 4 — `.planning/codebase/STACK.md`: align dependency table with `pyproject.toml`

<action>

1. In the **Core dependencies** table, add a row for **`python-dotenv`** (e.g. **Env / secrets** or **Configuration**) so it is **no longer** only in “Implicit”.
2. Remove or rewrite the **`## Implicit / environment dependencies`** subsection so it **does not** state that **`python-dotenv`** is missing from **`pyproject.toml`**. If you keep a short “Environment” note, it should say that **`.env`** is optional and loaded via **`load_dotenv`** where implemented—not that the package is undeclared.

</action>

<read_first>

- `.planning/codebase/STACK.md`
- `pyproject.toml`

</read_first>

<acceptance_criteria>

- `STACK.md` contains **`python-dotenv`** in the main dependency table.
- `rg 'not.*listed in.*pyproject|missing from.*pyproject' .planning/codebase/STACK.md` exits **1** (no match for “missing from pyproject” style wording for dotenv).

</acceptance_criteria>

---

## Task 5 — `.planning/codebase/CONCERNS.md`: dependency drift (factual)

<action>

1. Under **## Dependency drift**, update the bullet about **`python-dotenv`** so it states that **`python-dotenv`** is **declared** in **`pyproject.toml`** (post–Phase 5). Remove or rewrite any sentence claiming it is **not** declared.
2. Leave the **`pydantic`** bullet **unchanged** (no dep cleanup in this phase).

</action>

<read_first>

- `.planning/codebase/CONCERNS.md`
- `pyproject.toml`

</read_first>

<acceptance_criteria>

- `CONCERNS.md` contains **`python-dotenv`** and **`declared`** (or equivalent: “listed in `pyproject.toml`”) in the same bullet or sentence.
- `rg 'not.*declared|missing from pyproject' .planning/codebase/CONCERNS.md` — if it matches, it must **not** refer to **`python-dotenv`** (prefer zero matches for dotenv+missing).

</acceptance_criteria>

---

## Task 6 — `.planning/codebase/STRUCTURE.md`: tree and CLI commands (Phases 3–4)

<action>

1. Extend the repository tree so it includes:
   - **`src/contextcliff/import_bridge/`** (at minimum `__init__.py`, **`artifact_v1.py`**).
   - **`src/contextcliff/analysis/profile_report.py`** alongside **`binning.py`** and **`cliff.py`**.
2. Update **## Naming conventions** / CLI line: subcommands are **`prepare`**, **`run`**, **`import`**, **`profile`** (not only three commands).

</action>

<read_first>

- `.planning/codebase/STRUCTURE.md`
- `src/contextcliff/cli/main.py`

</read_first>

<acceptance_criteria>

- `STRUCTURE.md` contains **`import_bridge`** and **`profile_report.py`**.
- `STRUCTURE.md` lists the **`import`** subcommand alongside **`prepare`**, **`run`**, **`profile`**.

</acceptance_criteria>

---

## Task 7 — `.planning/codebase/ARCHITECTURE.md`: flow, layers, data path (Phases 3–4)

<action>

1. In **Purpose** (or opening paragraph), state the CLI flow **`prepare` → `run` → `import` (optional) → `profile`** — **import** loads external JSON artifacts into SQLite with provenance.
2. In **Layering**, add a bullet for **`import_bridge`**: parses **JSON v1** artifacts; CLI **`contextcliff import`** persists via **`StateManager`** (align wording with **`src/contextcliff/cli/main.py`** and **`import_bridge/artifact_v1.py`**).
3. In **Layering**, extend **Analysis** to mention **`profile_report`** helpers and optional **`--manifest`** / filter behavior at a **high level** (no API spec).
4. Update **Data flow** diagram or bullet list to include: external artifact → **`import`** → **`state.db`** with **imported** run labeling; **profile** reads SQLite and may join manifest metadata for reporting.

</action>

<read_first>

- `.planning/codebase/ARCHITECTURE.md`
- `src/contextcliff/cli/main.py`
- `.planning/phases/03-external-import-bridge/03-CONTEXT.md` (contract summary only)

</read_first>

<acceptance_criteria>

- `ARCHITECTURE.md` contains **`import`** (CLI) and **`import_bridge`** or **`artifact_v1`**.
- `ARCHITECTURE.md` contains **`profile_report`** or **`Profile report`** (referring to the analysis helper module).
- `ARCHITECTURE.md` mentions **imported** / **provenance** in the data-flow description (e.g. **`run_source`**, **external** labeling—use terms consistent with **`state.py`** / report headers).

</acceptance_criteria>

---

## Task 8 — `.planning/codebase/INTEGRATIONS.md`: import path + SQLite (DOC-03)

<action>

1. Add a subsection **## External JSON import (IMP-01)** (title flexible) covering:
   - **Where:** **`contextcliff import`** → **`import_bridge.artifact_v1.parse_artifact_v1`** → SQLite writes via **`StateManager`**.
   - **Artifact:** **`schema_version`** **1** JSON as defined in **`artifact_v1.py`** (high level).
2. Under **Local persistence**, add one sentence: **`runs`** rows carry **provenance** (**`run_source`**, **`external_label`**, **`artifact_ref`**) for internal vs imported runs.

</action>

<read_first>

- `.planning/codebase/INTEGRATIONS.md`
- `src/contextcliff/import_bridge/artifact_v1.py`
- `src/contextcliff/runner/state.py` (column names only)

</read_first>

<acceptance_criteria>

- `INTEGRATIONS.md` contains **`import_bridge`** or **`parse_artifact_v1`**.
- `INTEGRATIONS.md` contains **`run_source`** or **`imported`** in the SQLite / persistence discussion.

</acceptance_criteria>

---

## Task 9 — `.planning/codebase/CONVENTIONS.md` and `TESTING.md`: factual patches only

<action>

1. **`CONVENTIONS.md`**: In **CLI patterns**, list subcommands **`prepare`**, **`run`**, **`import`**, **`profile`** so it matches **`main.py`**.
2. **`TESTING.md`**: Replace outdated claims that there is **no** **`tests/`** directory. State that **`tests/`** exists with **`unittest`** modules (e.g. reference **`tests/test_import_bridge.py`**, **`tests/test_profile_report.py`**, **`tests/test_profile_phase4.py`** by name) and that **`uv run python -m unittest`** is the discovery command. Remove or narrow the “no unittest discovery” sentence so it is **false** no longer.

</action>

<read_first>

- `.planning/codebase/CONVENTIONS.md`
- `.planning/codebase/TESTING.md`
- `tests/`

</read_first>

<acceptance_criteria>

- `CONVENTIONS.md` contains **`import`** as a subcommand name next to **`prepare`**, **`run`**, **`profile`**.
- `TESTING.md` contains **`tests/`** and **`unittest`**.
- `rg 'No `tests/` directory' .planning/codebase/TESTING.md` exits **1** (no literal outdated “no tests/” claim—adjust wording if needed).

</acceptance_criteria>

---

## Verification criteria (phase)

- **`DEP-01`:** `pyproject.toml` lists **`python-dotenv`**; **`uv run`** smoke imports succeed; audit confirms third-party imports ⊆ declared deps.
- **`DOC-03`:** All seven files under **`.planning/codebase/`** are internally consistent with Phases 3–4 and **`pyproject.toml`**; no revival of “KV runtime shipped in-repo” as **current** behavior.

## must_haves (goal-backward)

1. **`python-dotenv`** is a declared runtime dependency and **`load_dotenv`** import sites remain valid under **`uv run`**.
2. **`.planning/codebase/`** documents **`import`**, **`import_bridge`**, **`profile_report`**, and provenance-aware SQLite **without** requiring changes to **`docs/architecture.md`** (DOC-03 scope is the codebase map only).
