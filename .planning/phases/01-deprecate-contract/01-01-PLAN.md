---
wave: 1
plan: 01-01
phase: 1
phase_slug: deprecate-contract
depends_on: []
parallelization: false
autonomous: true
requirements_addressed:
  - DOC-01
  - DOC-02
  - RUN-01
---

# Plan 01-01: Hard doc cleanup, README, architecture SoT, CLI contract

## Objective

Fulfill **DOC-01**, **DOC-02**, **RUN-01** per `01-CONTEXT.md`: **hard cleanup** (KV-era material in `docs/archive/`, no soft-only warnings), **minimal root `README.md`**, **`docs/architecture.md`** as execution/structure source of truth, **user-facing docs** link only to `docs/architecture.md` (no `.planning/` paths), **CLI** updated via module docstring + `@click.group(help=..., epilog=...)` + subcommand docstrings, **`Runner`** docstrings state API/mock-only path.

## Requirements addressed

| ID | How this plan covers it |
|----|-------------------------|
| DOC-01 | Move or archive misleading docs; remove/replace KV-runtime-as-shipped claims in active `docs/` |
| DOC-02 | `README.md` + expanded `docs/architecture.md` with explicit API/mock contract |
| RUN-01 | No new flags; docstrings/help state no KV controls; compression via external workflows + future import |

## Strategy

Single wave, sequential tasks. **No** new Python modules. **No** `.planning/` links in `README.md` or `docs/**/*.md` (excluding nothing—user-facing means all shipped markdown under repo root + `docs/` except `docs/archive/` which is historical).

---

## Task 1 — Inventory misleading content in `docs/` and `src/`

<action>
Run from repo root and save mental note for Task 2–3 (no commit required for grep output alone):

```bash
rg -n "kv_policy|SnapKV|PyramidKV|KVCache|KV cache|vllm|vLLM|local-engine|local engine|kv_budget|KVCache-Factory|monkeypatch|--kv" docs/ src/ --glob "*.md" --glob "*.py" 2>/dev/null || true
```

For each **active** doc file under `docs/` (not under `docs/archive/`) that matches, classify: **archive** (move whole file to `docs/archive/`), **rewrite** (strip KV sections in place), or **leave** if match is negation/warning only.
</action>

<read_first>
- `docs/architecture.md`
- `docs/specifications.md`
- `docs/research.md`
- `docs/prompts.md`
- `docs/prd.md`
- `docs/log.md`
- `01-CONTEXT.md` (this phase)
</read_first>

<acceptance_criteria>
- `rg` command above has been run; executor records a short list of files/lines to change in Task 2–3 (can be bullet list in execute SUMMARY).
- No file is edited in Task 1.
</acceptance_criteria>

---

## Task 2 — `docs/archive/` hard cleanup

<action>
1. Ensure directory exists: `docs/archive/` (add `docs/archive/README.md` optional one line: "Superseded or historical documents; see `docs/architecture.md` for current model." — **must not** reference `.planning/`).

2. **Move** (git mv) any **active** `docs/*.md` file that consists primarily of KV/vLLM-as-product narrative into `docs/archive/`. Per current tree, long-form KV content already lives under `docs/archive/` for several files; if `docs/specifications.md`, `docs/research.md`, `docs/prompts.md`, or `docs/prd.md` contain shipped KV runtime instructions, **move** them to `docs/archive/` preserving basename (e.g. `docs/archive/specifications.md`).

3. If a moved file is required to exist at old path for external links, replace old path with a **stub** `docs/<name>.md` containing only:
   - Title
   - One paragraph: content moved to `docs/archive/<name>.md`; current harness described in `docs/architecture.md`
   - **No** `.planning/` links

4. Delete stub approach if no links require it—prefer **move only** and update any internal references in remaining docs.
</action>

<read_first>
- Results of Task 1
- `docs/specifications.md`
- `docs/research.md`
- `docs/prd.md`
- `docs/prompts.md`
</read_first>

<acceptance_criteria>
- `docs/archive/` exists.
- After moves, `rg "kv_policy|SnapKV|PyramidKV" docs/*.md` (non-recursive) returns no hits **or** only hits in acceptable stub files that point to `docs/architecture.md` (show command output in SUMMARY).
- No user-facing file under `docs/` (excluding `docs/archive/`) contains the substring `.planning/` (verify: `rg "\.planning" docs/ --glob "*.md" | rg -v archive` → empty).
</acceptance_criteria>

---

## Task 3 — `docs/architecture.md` source of truth

<action>
Replace or prepend content so the file contains **all** of the following **verbatim concepts** (wording can vary slightly but grep must find these substrings):

- `## Current execution model`
- `API`
- `mock`
- `No in-repository KV`
- `import`

Structure:
1. Short title `# ContextCliff architecture`
2. Section `## Current execution model` — bullets: in-repo inference is **remote API** (OpenAI-compatible client) or **`mock`** for dry runs; **no** in-repo KV compression runtime; **no** CLI flags for `kv_budget` or SnapKV/PyramidKV; experiments with compressed KV run **outside** this repo; **import** of external results is **planned** (later phase)—one sentence.
3. Section `## Documentation map` — `README.md` → orientation; **this file** → detail; `docs/archive/` → historical only.
4. Optional: trimmed `## Repository layout` aligned with real `src/contextcliff/` tree (remove fictional `src/data/adapter.py` paths or mark outdated).

**Do not** add links to `.planning/`.
</action>

<read_first>
- `docs/architecture.md`
- `.planning/codebase/STRUCTURE.md`
</read_first>

<acceptance_criteria>
- `docs/architecture.md` contains lines matching: `## Current execution model`, `API`, `mock`, `No in-repository KV`, `import` (verify with `rg -n "Current execution model|No in-repository KV" docs/architecture.md`).
- File does not contain `.planning`.
</acceptance_criteria>

---

## Task 4 — Root `README.md` (DOC-02)

<action>
Create `README.md` at repository root with **exactly three** parts:

1. **Title** `# ContextCliff` (or `##` subtitle if you prefer one H1 only—use single `# ContextCliff` as H1).

2. **One paragraph** identity: Python CLI + SQLite for long-context QA degradation / cliff analysis (NLDA-style sampling, OpenAI API or mock, metrics, reports).

3. **Execution contract** paragraph: In-repo execution is **API or mock only**; no KV-cache compression controls in this CLI; for compression experiments use external tooling; harmonization via **import** (coming in a later milestone). Link to detail: `[docs/architecture.md](docs/architecture.md)` only.

**Forbidden:** any path containing `.planning`, `SnapKV` as something users run here, or `kv_policy`.
</action>

<read_first>
- `pyproject.toml` (project `description` for alignment)
- `01-CONTEXT.md` decisions D-03, D-04, D-07
</read_first>

<acceptance_criteria>
- `test -f README.md` is true.
- `rg "\.planning" README.md` exits 1 (no matches).
- `rg "docs/architecture\.md" README.md` exits 0.
- `rg -i "api|mock" README.md` exits 0.
</acceptance_criteria>

---

## Task 5 — CLI: `src/contextcliff/cli/main.py`

<action>
1. Replace module docstring at top with 4–6 lines stating: dispatcher for prepare/run/profile; **API or mock** execution only; **no** KV compression flags; see `docs/architecture.md`.

2. Replace bare `@click.group()` with explicit group including **help** and **epilog**:
   - `help="ContextCliff — long-context QA evaluation (prepare → run → profile)."`
   - `epilog="Execution: remote API (e.g. OpenAI) or --model mock. No KV-cache or compression controls. Details: docs/architecture.md"`

3. Expand `main()` docstring if needed so `contextcliff --help` shows group help (Click uses group callback docstring for `main`—set on `@click.group()` via `help` parameter; the `main()` function body can stay `pass` with docstring on `def main` for some Click versions—prefer putting **short** description on the group: use `@click.group(help="...", epilog="...")` and keep `def main(): \"\"\"...\"\"\"` empty or mirror).

   **Concrete:** Use `click.group()` kwargs: `help="..."`, `epilog="..."` as in epilog string above.

4. Add one line to `prepare`, `run`, `profile` docstrings if any imply local KV (e.g. "Uses API or mock per manifest").

5. **Do not** add new options or imports beyond `click` (already present).
</action>

<read_first>
- `src/contextcliff/cli/main.py`
</read_first>

<acceptance_criteria>
- `python -m contextcliff.cli.main --help` or `contextcliff --help` (if installed) shows epilog text containing `mock` and `docs/architecture.md` (paste excerpt in SUMMARY).
- `rg "@click\.group" src/contextcliff/cli/main.py` shows `help=` and `epilog=` on same decorator.
- `rg "kv_policy|SnapKV" src/contextcliff/cli/main.py` exits 1.
</acceptance_criteria>

---

## Task 6 — Runner: `src/contextcliff/runner/engine.py`

<action>
1. Add module-level docstring after imports block (or top): states `Runner` uses `ModelClient` (**OpenAI** API or **MockClient**); not for local KV compression.

2. Expand class docstring `Runner` to: loads manifest JSON, runs **API or mock** generation loop, writes SQLite; **no** KV budget or compression hooks.

3. Do **not** change runtime logic beyond docstrings/comments.
</action>

<read_first>
- `src/contextcliff/runner/engine.py`
</read_first>

<acceptance_criteria>
- `rg "ModelClient|mock|API" src/contextcliff/runner/engine.py` finds matches in docstrings.
- `rg "KV|kv_budget|SnapKV" src/contextcliff/runner/engine.py` exits 1 or only matches in comments that say "no KV" (prefer no KV tokens at all except negation).
</acceptance_criteria>

---

## Task 7 — Final verification (DOC-01 / RUN-01)

<action>
Run and record output in phase SUMMARY:

```bash
rg -n "kv_policy|SnapKV|PyramidKV|KVCache-Factory|vllm\s+run|--kv_policy" docs/ README.md src/ --glob "*.md" --glob "*.py" 2>/dev/null | rg -v "docs/archive" || true
```

Expect: **no** misleading standalone claims in active `docs/` (archive may still contain historical terms). For `src/`, expect no KV policy flags.

Second check:

```bash
rg "\.planning/" README.md docs/ --glob "*.md" | rg -v archive || true
```

Expect: empty.
</action>

<read_first>
- `01-CONTEXT.md`
</read_first>

<acceptance_criteria>
- Both commands run; SUMMARY documents exit status and line counts.
- Executor confirms DOC-01, DOC-02, RUN-01 manually against REQUIREMENTS checklist.
</acceptance_criteria>

---

## Verification criteria (phase)

1. User-facing docs: no `.planning/`; README points to `docs/architecture.md`.
2. `docs/architecture.md` contains **Current execution model** with API/mock, no in-repo KV, import path forward reference.
3. CLI `--help` shows contract epilog; runner docstrings describe API/mock only.

## must_haves (goal-backward)

- [ ] **DOC-01:** Active `docs/` tree does not sell in-repo KV runtime; archive holds historical junk.
- [ ] **DOC-02:** `README.md` + `docs/architecture.md` satisfy contract and map.
- [ ] **RUN-01:** CLI/runner text surfaces match API-only harness; no KV controls added.

## Notes

- **plan_checker:** Self-verify against `<quality_gate>` (read_first + acceptance_criteria on every task).
- If `pytest` or packaging breaks: fix only if caused by docstring/import changes (unlikely).
