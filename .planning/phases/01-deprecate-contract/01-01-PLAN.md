---
wave: 1
plan: 01-01
phase: 1
phase_slug: deprecate-contract
depends_on: []
parallelization: false
autonomous: true
---

# Plan 01-01: Deprecate KV-runtime docs & lock API contract

## Requirements addressed

- **DOC-01** — Misleading in-repo KV / vLLM / compression-as-shipped narrative removed or explicitly deprecated in listed docs.
- **DOC-02** — Canonical statement of API/mock-only execution + external import for compression studies.
- **RUN-01** — No CLI or runner surface implies in-repo KV compression or `kv_budget`-style API controls.

## Strategy

**Removal before addition:** Edit documentation and comments first; do not add new modules. Verify `src/` with ripgrep. Touch `pyproject.toml` only if a user-visible description is misleading (optional one-line).

## Tasks

<task id="1" name="Inventory misleading strings">
  <action>Search `docs/` and `src/` for patterns: `kv_policy`, `SnapKV`, `PyramidKV`, `KVCache`, `vllm`, `vLLM`, `local-engine`, `local engine`, `kv_budget`, `H2O` (in KV context), `monkeypatch`.</action>
  <output>List of files and line ranges to edit.</output>
</task>

<task id="2" name="Canonical execution model">
  <action>Add to `docs/architecture.md` a **Current execution model (2026 reset)** section: in-repo runs use `ModelClient` implementations that call remote APIs (`OpenAIClient`) or `mock`; no in-repo KV compression runtime; future/optional **import** of external experiment artifacts (detailed in later phases).</action>
  <output>Updated `docs/architecture.md` with accurate scope; keep existing structure where it does not contradict.</output>
</task>

<task id="3" name="Deprecate long-form design docs">
  <action>For each of `docs/full_desc.md`, `docs/blueprint.md`, `docs/architecture_preview.md`, `docs/data_layer.md`: add a top-of-file **Deprecated / aspirational** notice where content describes local vLLM, `--kv_policy`, or in-process compression as if implemented; point to `docs/architecture.md` and `.planning/PROJECT.md`. Trim or rewrite opening paragraphs if they assert shipped behavior.</action>
  <output>Each file either clearly historical or aligned with API-only harness.</output>
</task>

<task id="4" name="CLI and runner review">
  <action>Read `src/contextcliff/cli/main.py` and `src/contextcliff/runner/engine.py`. Ensure module docstrings and `click` help strings do not promise local KV engines or compression flags. Fix wording only.</action>
  <output>No misleading user-facing strings; RUN-01 satisfied for current code surface.</output>
</task>

<task id="5" name="Verification pass">
  <action>Re-run search from task 1 on `docs/` and `src/`; confirm no unqualified “you can run SnapKV here” claims. Manual read of canonical section in `docs/architecture.md`.</action>
  <output>Short verification note in phase SUMMARY (post-execute) or checklist below.</output>
</task>

## Verification criteria

1. `docs/architecture.md` states API/mock-only in-repo execution and points to project scope for import.
2. Listed long docs do not present in-repo compression runtime as current product without deprecation.
3. CLI and runner contain no options or help text for KV policies, `kv_budget`, or local compression engines.

## must_haves (goal-backward)

- [ ] **DOC-01:** Targeted docs are honest or explicitly deprecated.
- [ ] **DOC-02:** Canonical execution model is readable in `docs/architecture.md`.
- [ ] **RUN-01:** `src/` CLI surface reviewed; no new KV hooks introduced.

## Notes

- Do **not** implement import or schema changes in this plan (Phase 2+).
- Do **not** add `README.md` unless required for DOC-02; `docs/architecture.md` is the canonical home until README exists.
